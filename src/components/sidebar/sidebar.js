import { renderStartView } from '../chat/chat_start.js';


// Debugging
window.addEventListener('DOMContentLoaded', () => {
});


window.addEventListener('DOMContentLoaded', () => {
  renderStartView();
});
